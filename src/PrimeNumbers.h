#ifndef PRIME_NUMBERS_H
#define PRIME_NUMBERS_H

#include <stdio.h>
#include "GlobalDefs.h"

/**
 * @brief A prime number is a whole number greater than 1 whose only factors are 1 and itself
 * 
 * @param number 
 * @return BOOLEAN 
 */
extern BOOLEAN is_prime_number(int number);

#endif // PRIME_NUMBERS_H